#!/bin/bash

# Gera dinamicamente um ficheiro de configuração
cat << EOF > /etc/config.json
{
	"deviceId": 1,
	"apiBaseUrl": "http://localhost:8000",
	"examDirectory": "/home/student/Área de Trabalho/Exam"
}

EOF

